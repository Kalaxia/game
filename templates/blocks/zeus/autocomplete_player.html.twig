{% for player in players %}
	{# @TODO check the use of this statement #}
	{# if (in_array($player->getStatement(), array(PlayerStatement::Active, PlayerStatement::Inactive, PlayerStatement::Holiday, PlayerStatement::Banned))) { #}

	<twig:Avatar :player="player" class="!w-16 !h-16" />
	<span class="value-2">
		<twig:PlayerStatus :player="player" />
	</span>
	<span class="value-1"><span class="ac_value" data-id="{{ player.id }}">{{ player.name }}</span></span>
{% endfor %}
