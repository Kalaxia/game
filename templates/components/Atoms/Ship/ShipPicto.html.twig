<img src="{{ asset('images/legacy/picto/ships/types/' ~ get_ship_info(this.shipCategory, 'picto') ~ '.svg') }}"
	{{ attributes.defaults({
		'class': "w-6 md:mr-2",
	}) }}
	alt="{{ get_ship_info(this.shipCategory, 'name') }}" />
